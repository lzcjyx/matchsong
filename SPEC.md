# SPEC.md — MVP 产品需求规格说明书

- **里程碑：** M0（MVP 与架构冻结）
- **状态：** DRAFT → 待批准
- **版本：** 0.1.0
- **日期：** 2026-07-31
- **依据：** PLAN.md §2（产品与技术基线）、M-1 研究结论（docs/research/、docs/experiments/、docs/decisions/ADR-001..003）

---

## 1. 产品目标

**一句话定位：** 用户录制约 15-30 秒演唱，应用在端侧分析其本次录音的稳定演唱音区、舒适音区和音高稳定性，推荐音域适合用户演唱的歌曲，并给出可解释的推荐理由。

MVP 核心价值：

1. **可解释**：推荐理由由实测音频数据生成（"大部分旋律在你的舒适音区"），不是黑箱；
2. **端侧优先**：录音、分析、推荐全部在设备本地完成，原始音频默认不保存、不上传；
3. **诚实**：所有结果明确标注为"本次录音的估计"，不做音色类型、职业潜力、嗓音健康等绝对化结论；
4. **差异化**：填补"唱几句 → 推荐适合音域的歌"这一竞品不充分的空隙（见 product-opportunities.md）。

---

## 2. 目标用户

### 2.1 主要用户（MVP）

- **普通 K 歌爱好者**：去 KTV 或使用 K 歌应用，常因选歌不适而唱不上去/唱不下去；
- **缺乏乐理知识但想找到适合歌曲的用户**：不知道自己的音域，希望用最少操作获得"我能唱的歌"；
- 年龄 16-50 岁，使用 Android 手机（Android 8.0+/API 26+），中文或英文曲库使用者。

### 2.2 非主要用户（MVP 不服务）

- 专业歌手/声乐学习者（需要精确音域测量与教学反馈）；
- 需要嗓音健康评估的用户（明确排除，见非目标）。

---

## 3. 非目标（MVP 明确不做）

以下功能**不在 MVP 范围**，进入 Backlog：

| # | 非目标 | 原因 |
|---|---|---|
| N-1 | 音色/嗓音类型识别（男高音、女中音、"你的声音像某歌手"） | PLAN §2.1 禁止无研究依据的绝对结论 |
| N-2 | 唱歌评分（音准打分、K 歌 PK、社区社交） | 定位差异（竞品研究），评分产品密集且成熟 |
| N-3 | 演唱教学（共鸣、气息、咬字反馈） | 超出 MVP 范围 |
| N-4 | 嗓音疾病/健康诊断 | 医学边界，禁止 |
| N-5 | 云端实时音频分析 | PLAN §2.2 默认本地处理 |
| N-6 | 上传/分享原始录音 | 隐私策略：默认不上传 |
| N-7 | 伴奏录音混音、修音（pitch correction） | 属于 Voloco 等修音产品定位 |
| N-8 | 多用户账号系统 | 单机应用即可满足 MVP |
| N-9 | TFLite/端侧模型（CREPE 等） | M-1.5 Spike 证明纯 Kotlin YIN 已满足 |
| N-10 | 原生 C++/NDK 实现 | 性能证明不足前不引入（PLAN §2.2） |

---

## 4. 用户流程

### 4.1 主流程（Happy Path）

```text
启动 App
  → Onboarding（首次）：隐私与录音说明 → 同意
  → 首页（"开始测试"入口）
  → 录音准备页（选择测试方式：清唱 / 跟唱音阶提示；显示 15-30s 提示）
  → 麦克风权限申请（首次）
  → 录音页（倒计时 3s → 录音 15-30s，实时音量反馈，可提前停止）
  → 音频质量检测（自动，1-3s）
  → 分析中页面（YIN 音高分析 + 音域估计，进度展示）
  → 声音结果页（本次稳定音域、舒适音区、音高分布、置信度）
  → 推荐列表页（按适唱度排序的歌曲 + 推荐理由 + 变调建议）
  → 推荐详情页（歌曲信息、推荐理由明细、变调前后音域对比、收藏/反馈）
```

### 4.2 辅助流程

- **收藏**：推荐详情 → 收藏 → 收藏列表可见；
- **历史记录**：首页 → 历史 → 查看过去的分析摘要（不含原始音频）；
- **重新测试**：任意结果页 → "重新录制"；
- **设置**：偏好（语言/风格）、数据管理（删除历史/缓存/全部数据）。

### 4.3 流程约束

- 录音页必须显示前台通知（后台录音合法化）；
- 质量检测不合格 → 不进入分析，提示原因并建议重录；
- 数据不足（有效演唱 < 阈值）→ 提示重录，不生成正式推荐。

---

## 5. 功能需求

编号规则：`FR-<模块>-<序号>`。验收条件见 §12（Given/When/Then）。

### 5.1 模块 A：Onboarding 与隐私说明（FR-ONB）

| ID | 需求 | 优先级 |
|---|---|---|
| FR-ONB-1 | 首次启动展示 Onboarding，说明：为什么需要麦克风、录音用于什么、是否上传音频（否）、是否保存音频（默认否，临时缓存）、如何删除数据、结果非医学/专业诊断 | P0 |
| FR-ONB-2 | 用户必须显式同意隐私说明后才能进入主界面；同意状态持久化 | P0 |
| FR-ONB-3 | 同意后不再次展示 Onboarding（除非清除数据） | P0 |

### 5.2 模块 B：录音（FR-REC）

| ID | 需求 | 优先级 |
|---|---|---|
| FR-REC-1 | 使用 AudioRecord 采集 44.1kHz / 16bit / mono PCM（源：VOICE_RECOGNITION） | P0 |
| FR-REC-2 | 录音时长 15-30 秒，用户可提前停止；倒计时 3 秒 | P0 |
| FR-REC-3 | 录音期间前台通知可见（App 切后台录音继续） | P0 |
| FR-REC-4 | 实时音量反馈（音量条 + 过低/削波提示），更新节流 ≤10Hz | P0 |
| FR-REC-5 | 麦克风权限状态机：未请求/请求中/已授予/已拒绝/永久拒绝/不可用，处理"不再询问"与从设置返回 | P0 |
| FR-REC-6 | 录音状态机：Idle/Preparing/Countdown/Recording/Stopping/Completed/Failed（MVP 无 Pause） | P0 |
| FR-REC-7 | PCM 临时写入 cache 目录；录音结束立即生成 WAV（含 header）供分析 | P0 |
| FR-REC-8 | 录音失败/崩溃后清理残留文件；下次启动清理过期缓存 | P0 |
| FR-REC-9 | 不得静默录音：任何录音必须伴随可见 UI + 前台通知 | P0 |

### 5.3 模块 C：音频质量检测（FR-QUAL）

| ID | 需求 | 优先级 |
|---|---|---|
| FR-QUAL-1 | 对录音输出 AudioQualityReport：时长、静音比例、平均 RMS、峰值、削波比例、有效声音比例、近似噪声水平、可分析帧数、isUsable、confidence | P0 |
| FR-QUAL-2 | 阈值集中配置（静音阈值、低音量阈值、最小有效声音时长、最小有效帧比例、削波比例上限） | P0 |
| FR-QUAL-3 | 不合格录音不得进入正式分析；按原因给出明确提示（过短/无声/太小/嘈杂/削波/有效片段不足）+ 建议重录 | P0 |
| FR-QUAL-4 | 质量检测输入支持：实时 PCM、WAV 测试文件、Fake Frame Source | P1 |

### 5.4 模块 D：音高与音域分析（FR-ANAL）

| ID | 需求 | 优先级 |
|---|---|---|
| FR-ANAL-1 | YIN 音高检测（纯 Kotlin，65-1046Hz，帧 2048@44.1k，hop 1024），接口独立、可注入、可取消 | P0 |
| FR-ANAL-2 | 音高后处理：无效/低置信度帧过滤、八度错误近似处理、中值滤波、短时跳变过滤、频率↔MIDI↔音名 | P0 |
| FR-ANAL-3 | 稳定音域估计：异常值剔除 + 分位数（如 P5/P95），不得直接取全部帧极值；输出稳定最低/最高音、覆盖范围、置信度、样本充足性 | P0 |
| FR-ANAL-4 | 舒适音区估计：基于音高分布/停留时间/稳定音符比例/边缘样本数；输出舒适最低/最高音、主要演唱音区、置信度 | P0 |
| FR-ANAL-5 | 音高稳定性：稳定片段比例、音高波动、长音波动、有效帧比例；不输出"唱功分数" | P0 |
| FR-ANAL-6 | 输出 VoiceAnalysisResult：质量报告 + 音高轨迹 + 稳定音域 + 舒适音区 + 稳定性 + 置信度 + 警告 + 算法版本 | P0 |
| FR-ANAL-7 | 结果页面通俗展示（避免裸算法指标），含"本次录音估计"声明、数据不足提示、重录入口 | P0 |
| FR-ANAL-8 | 数据不足时拒绝过度推断（有效帧 < 阈值 → 不输出音域/推荐） | P0 |

### 5.5 模块 E：歌曲数据（FR-SONG）

| ID | 需求 | 优先级 |
|---|---|---|
| FR-SONG-1 | 歌曲 Schema：ID、名称、歌手、语言、风格、原调、最低/最高音（MIDI）、主要音区、音域跨度、高音持续负担、长音负担、跳进难度、节奏难度、总体难度、推荐变调范围、试听/外部链接、数据来源、可信度、数据版本 | P0 |
| FR-SONG-2 | MVP 数据集 50-200 首，覆盖不同音区/男女歌手/语言/风格；每首必须有来源或可信度声明，不得批量虚构 | P0 |
| FR-SONG-3 | 数据导入工具（JSON/CSV）与 App 解耦：字段校验、重复/音域/来源/版本检查、错误报告 | P1 |
| FR-SONG-4 | Room 存储：Entity/DAO/版本管理/初始导入/升级/搜索/筛选/收藏关系 | P0 |
| FR-SONG-5 | 所有音高字段统一使用 MIDI Note 内部标准 | P0 |

### 5.6 模块 F：推荐引擎（FR-RECM）

| ID | 需求 | 优先级 |
|---|---|---|
| FR-RECM-1 | 候选过滤：语言、用户排除风格、超出可调整音域、数据不完整；不以歌手性别硬过滤 | P0 |
| FR-RECM-2 | 变调计算：每候选计算原调匹配度、升降半音数、变调后最低/最高音/主要音区、是否超出合理变调范围（默认 ±6 半音，可配置） | P0 |
| FR-RECM-3 | 评分模型：RangeFit / TessituraFit / HighNoteBurdenFit / DifficultyFit / PitchStabilityFit / PreferenceFit / ConfidenceAdjustment；权重集中配置且记录版本 | P0 |
| FR-RECM-4 | 推荐解释由实际评分特征生成（模板 + 数据填充），禁止无数据文案 | P0 |
| FR-RECM-5 | 无高匹配时降级：扩大风格、建议变调、展示接近匹配、说明原因，不伪造高分 | P0 |
| FR-RECM-6 | 低置信度用户分析降权（ConfidenceAdjustment），不得当高精度输入 | P0 |
| FR-RECM-7 | 推荐结果可重复（相同输入相同排序） | P0 |

### 5.7 模块 G：历史、收藏、反馈（FR-HX）

| ID | 需求 | 优先级 |
|---|---|---|
| FR-HX-1 | 历史记录默认只保存：分析摘要、时间、音域结果、推荐结果引用、算法版本（不含原始音频） | P0 |
| FR-HX-2 | 收藏/取消收藏歌曲，收藏列表可查看 | P0 |
| FR-HX-3 | 用户反馈：适合唱/太高/太低/太难/不喜欢风格/推荐理由不准确；仅保存，MVP 不自动调权重 | P1 |
| FR-HX-4 | 数据删除：单条历史、全部历史、收藏、设置、缓存音频、重置应用 | P0 |

### 5.8 模块 H：隐私与安全（FR-PRIV）

| ID | 需求 | 优先级 |
|---|---|---|
| FR-PRIV-1 | 原始录音默认临时保存（cache），分析完成后删除 | P0 |
| FR-PRIV-2 | 用户主动保存/分享录音时必须明确提示且二次确认 | P1 |
| FR-PRIV-3 | 无任何上传原始音频或声音特征的网络路径（MVP 无后端） | P0 |
| FR-PRIV-4 | Release 日志脱敏（不含文件名、路径、设备标识、音频内容） | P0 |
| FR-PRIV-5 | 数据删除流程完整可测（单条/全部/缓存/重置） | P0 |

### 5.9 模块 I：应用外壳与导航（FR-SHELL）

| ID | 需求 | 优先级 |
|---|---|---|
| FR-SHELL-1 | Navigation Compose 路由覆盖全部 MVP 页面（启动/Onboarding/首页/录音准备/录音/质量结果/分析中/声音结果/推荐列表/推荐详情/收藏/历史/设置/删除确认） | P0 |
| FR-SHELL-2 | Design System：Typography/Spacing/Shape/组件 + Loading/Empty/Error/Permission/质量警告状态，业务页不得硬编码样式 | P0 |
| FR-SHELL-3 | Fake 数据流程可串联全流程（Fake Repository），明确标记测试数据，不得进入 Release | P0 |

---

## 6. 异常流程

| 场景 | 行为 |
|---|---|
| 权限拒绝（首次） | 展示权限说明页，可重试；拒绝后停留在录音准备页 |
| 权限永久拒绝 | 引导去系统设置开启；从设置返回后刷新状态 |
| 权限使用中撤销 | 录音状态机 → Failed，提示重录，清理缓存 |
| 录音初始化失败（无麦克风/占用） | 明确错误提示 + 重试入口 |
| 录音过短（< 阈值） | 质量失败：提示"录音过短，请至少演唱 10 秒" |
| 无有效声音 / 过小 / 嘈杂 / 削波 | 质量失败：对应提示 + 建议（靠近麦克风/安静环境/降低音量） |
| 分析取消/崩溃 | 恢复流程：回到录音页或重试，不产生半成品结果 |
| 分析数据不足 | 显示"有效演唱片段不足"，建议重录，不生成推荐 |
| 推荐无结果 | 降级展示（见 FR-RECM-5），说明原因 |
| 存储不足 | 录音前检查可用空间；失败时提示清理 |
| App 进程被系统重建 | 恢复当前流程（保存的 Session 状态），不丢失关键步骤 |
| 数据库失败 | 显示错误 + 重试；不崩溃 |

---

## 7. 推荐系统定义（MVP）

### 7.1 推荐流程

```text
VoiceAnalysisResult（用户）
+ UserSettings（语言/风格偏好）
+ SongMetadata（候选歌曲）
→ CandidateFilter（候选过滤）
→ KeyShiftEvaluation（变调评估）
→ FeatureScoring（特征评分）
→ Ranking（排序）
→ ExplanationGeneration（解释生成）
→ RecommendationResult（推荐结果，Top N + 理由 + 变调建议）
```

### 7.2 评分特征（权重集中配置，记录版本）

| 特征 | 定义 | 默认权重（v1，待校准） |
|---|---|---|
| RangeFit | 变调后歌曲最低/最高音 vs 用户稳定音域的重合度 | 0.30 |
| TessituraFit | 变调后歌曲主要音区 vs 用户舒适音区重合度 | 0.25 |
| HighNoteBurdenFit | 歌曲高音持续负担 vs 用户高音区稳定性 | 0.15 |
| DifficultyFit | 歌曲总体难度 vs 用户稳定性指标 | 0.10 |
| PitchStabilityFit | 旋律跳进/长音负担 vs 用户音高稳定性 | 0.10 |
| PreferenceFit | 语言/风格偏好匹配 | 0.10 |
| ConfidenceAdjustment | 用户分析置信度对总分的乘性降权（confidence<0.5 时显著降权） | 乘子 |

评分范围 0-100，可解释模板由每个特征的实际值触发（见 FR-RECM-4）。

### 7.3 推荐输出

```text
RecommendationResult:
- recommendations: List<RecommendationItem>（Top 10）
- RecommendationItem: song, score, keyShiftSemitones, explanation: List<String>,
  fitBreakdown: Map<Feature, FitLevel>
- totalConfidence: Low/Medium/High
- emptyStateReason: String?（无结果时）
```

### 7.4 置信度与失败状态定义

| 级别 | 判定 | 行为 |
|---|---|---|
| High | 有效帧 ≥ 阈值且质量报告可用 | 正常推荐 |
| Medium | 有效帧中等 | 推荐 + 标注"结果基于有限样本" |
| Low | 有效帧不足/质量可疑 | 不生成正式推荐，提示重录 |
| Failed | 质量检测失败 | 不进入分析（§6 异常流程） |

**核心原则：不可靠音频不得生成正式结果。**

---

## 8. 数据模型（概要）

完整字段定义（类型/单位/合法范围/可空/来源/保存位置/敏感/保留时间）见 `docs/architecture/data-model.md`（M0.3 交付物）。

核心模型清单（与 PLAN M0.3 一致）：

```text
RecordingSession     录音会话（状态、配置、时间线）
RecordingConfig      录音配置（采样率/声道/编码/时长上下限）
AudioQualityReport   质量报告（§5.3）
PitchFrame           单帧音高（时间戳、f0Hz、置信度、RMS）
PitchTrack           音高轨迹（帧列表、方法、版本）
VocalRangeEstimate   稳定音域估计（最低/最高/分位数/置信度）
VoiceFeatureVector   声音特征向量（供推荐消费）
SongMetadata         歌曲元数据（§5.5 FR-SONG-1）
SongRangeProfile     歌曲音域画像（变调后的音域映射）
RecommendationScore  推荐分数（特征分解）
RecommendationExplanation  推荐解释（特征→文案）
RecommendationResult 推荐结果
UserFeedback         用户反馈（§5.7 FR-HX-3）
UserSettings         用户设置（语言/风格偏好、数据管理）
ConsentRecord        隐私同意记录（版本、时间、状态）
```

---

## 9. Android 架构（概要）

完整架构（模块划分、依赖方向、线程策略、错误模型、日志策略）见 `ARCHITECTURE.md`（M0.2 交付物）。

MVP 采用**模块化单工程**（逻辑边界清晰，Gradle 模块适度精简）：

```text
app                    UI（Compose）+ 导航 + DI 装配
core:common            错误模型、Dispatcher、时钟、Logger
core:model             纯 Kotlin 数据模型（无 Android 依赖）
core:audio             音频采集/分析引擎（AudioRecord 封装、YIN、质量检测）
core:testing           测试工具（Fake 数据工厂、音频夹具）
data:local             Room + DataStore + 文件缓存
data:songs             歌曲数据（导入、校验、版本）
domain:recording       录音用例与状态机
domain:analysis        分析用例（质量→音高→音域）
domain:recommendation  推荐用例（评分、解释）
feature:onboarding     页面
feature:recording      页面
feature:analysis       页面
feature:recommendation 页面
feature:history        页面
feature:settings       页面
```

依赖方向：`feature → domain → core`，`data` 实现 domain 接口；UI 不直接依赖 Audio Engine 实现类。

---

## 10. 隐私与安全

1. **数据清单**（详细版见 M9.1 交付物）：收集项 = 录音（临时）、声音特征（音高轨迹/音域统计）、偏好设置、历史摘要；
2. **原始音频**：默认 cache 临时保存，分析完成即删；不自动上传；用户主动保存需明确同意；
3. **网络**：MVP 无任何网络权限与后端，无 API Key；
4. **日志**：Release 脱敏，不含路径/设备标识/音频内容；
5. **权限最小化**：仅 RECORD_AUDIO + FOREGROUND_SERVICE(+_MICROPHONE)，无其他危险权限；
6. **同意记录**：ConsentRecord 持久化隐私说明版本与同意时间，隐私说明变更需重新同意（M9 细化）；
7. **合规**：Play Store 数据安全表单、隐私政策、麦克风权限用途说明（M9/M11）。

---

## 11. 非功能需求

| 类别 | 需求 | 验收指标 |
|---|---|---|
| 性能-分析 | 30 秒录音的 YIN 分析耗时 | ≤ 10 秒（中端设备，SPEC 目标；Spike 桌面实测 ~1ms/帧，30s 音频按 hop 1024 ≈ 1292 帧 → 约 1.3s，余量充足） |
| 性能-录音 | 录音期间 UI 流畅 | 无卡顿（音量反馈 ≤10Hz 更新） |
| 性能-启动 | 冷启动到首页 | ≤ 3 秒（M10 基准） |
| 内存 | 分析峰值内存 | ≤ 200MB（M10 基准） |
| 电池 | 录音+分析耗电 | 单次完整流程 ≤ 1% 电量（M10 基准） |
| 兼容 | Android 版本 | minSdk 26（Android 8.0），targetSdk 36 |
| 兼容 | 设备 | 至少覆盖：低端/中端/Pixel/Samsung/中国厂商各 1 台（M10.3 矩阵） |
| 可靠性 | 崩溃率 | 无 P0/P1 Bug；正式发布前已知崩溃率 0 |
| 可维护性 | 静态检查 | Lint/Detekt/Ktlint 通过（M1 配置） |
| 可测试性 | 单元测试覆盖率 | 核心逻辑（domain/core:audio/core:model）≥ 80%（行覆盖率） |
| 数据 | 歌曲数据 | MVP 50-200 首，全部通过自动校验，有来源 |
| 隐私 | 删除 | 用户可删除全部个人数据，删除后有确认 |

---

## 12. Given/When/Then 验收条件（MVP 关键场景）

### ACC-1 首次启动隐私同意
- **Given** 全新安装用户启动 App
- **When** Onboarding 展示并选择"同意"
- **Then** 进入首页；重启后不再展示 Onboarding；ConsentRecord 持久化

### ACC-2 隐私拒绝
- **Given** Onboarding 展示中
- **When** 用户选择"不同意"
- **Then** 停留在 Onboarding；不进入首页；不请求任何权限；不采集任何音频

### ACC-3 权限流程
- **Given** 已同意隐私说明、进入录音准备页
- **When** 用户点击"开始测试"
- **Then** 请求麦克风权限；授予后进入录音页；拒绝后展示权限说明并可重试；永久拒绝后引导设置

### ACC-4 正常录音 15-30s
- **Given** 权限已授予、录音页倒计时结束
- **When** 用户演唱 20 秒后自动停止（或手动停止）
- **Then** 生成 PCM/WAV 临时文件；前台通知全程可见；录音状态 Completed；可进入质量检测

### ACC-5 后台录音持续
- **Given** 录音进行中
- **When** 用户按 Home 切后台 5 秒后返回
- **Then** 录音未中断；通知可见；继续录制直至停止

### ACC-6 质量合格进入分析
- **Given** 质量检测完成且 isUsable=true
- **When** 用户点击"查看分析"
- **Then** 展示分析进度；完成后展示声音结果页（稳定音域/舒适音区/置信度）

### ACC-7 静音录音被拒绝
- **Given** 录音内容为纯静音
- **When** 质量检测运行
- **Then** 输出 isUsable=false；提示"没有检测到声音"；不进入分析；建议重录

### ACC-8 削波录音被拒绝
- **Given** 录音削波比例超阈值
- **When** 质量检测运行
- **Then** isUsable=false；提示"麦克风削波"；不生成正式结果

### ACC-9 数据不足不推断
- **Given** 有效演唱帧不足阈值
- **When** 分析运行
- **Then** 不输出稳定音域/舒适音区；提示"有效演唱片段不足，请重录"；不生成推荐

### ACC-10 正常分析输出
- **Given** 合格录音完成分析
- **When** 声音结果页加载
- **Then** 显示稳定最低/最高音（音名）、舒适音区、音高分布、置信度、算法版本；标注"本次录音估计"

### ACC-11 推荐可解释
- **Given** 用户分析结果 + 歌曲数据就绪
- **When** 推荐列表加载
- **Then** 每首歌显示分数 + ≥1 条由实际特征生成的解释 + 变调建议（如有）

### ACC-12 推荐无结果降级
- **Given** 无候选满足最低匹配阈值
- **When** 推荐流程运行
- **Then** 展示空状态 + 原因说明 + 建议（扩大风格/重录）；不展示伪造高分

### ACC-13 结果可重复
- **Given** 相同 VoiceAnalysisResult + 相同歌曲数据 + 相同权重版本
- **When** 推荐引擎运行两次
- **Then** 两次排序与分数完全一致

### ACC-14 原始音频不保存
- **Given** 完成一次完整分析流程
- **When** 检查缓存目录
- **Then** 原始 PCM/WAV 已被删除；仅保留派生特征与历史摘要

### ACC-15 数据删除
- **Given** 存在历史记录/收藏/缓存
- **When** 用户在设置中执行"删除全部数据"
- **Then** 历史、收藏、缓存音频、设置全部清除；再次启动恢复首次启动状态（重新 Onboarding）

### ACC-16 推荐解释与分数一致
- **Given** 某歌曲得分主要由 TessituraFit 贡献
- **When** 查看该歌曲解释
- **Then** 解释包含舒适音区相关文案；不得出现与分数矛盾的解释

### ACC-17 变调建议有效性
- **Given** 歌曲原调最高音超出用户稳定高音
- **When** 推荐引擎评估该歌曲
- **Then** 输出降调建议（如 -2 半音）；变调后最高音落入用户音域；或标记不可调

---

## 13. 结果置信度与失败状态（汇总）

| 状态 | 定义 | 用户可见行为 |
|---|---|---|
| 质量失败 | isUsable=false | 原因提示 + 重录（§6） |
| 分析成功-高置信 | confidence ≥ 0.7 | 正常结果 + 推荐 |
| 分析成功-中置信 | 0.5 ≤ confidence < 0.7 | 结果 + "基于有限样本"标注 |
| 分析成功-低置信 | confidence < 0.5 | 不生成正式推荐（ACC-9） |
| 分析失败 | 异常/取消 | 错误提示 + 重试（§6） |

---

## 14. 变更记录

| 版本 | 日期 | 变更 |
|---|---|---|
| 0.1.0 | 2026-07-31 | 初稿，基于 M-1 研究结论与 ADR-001..003 |
