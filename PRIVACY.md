# 隐私说明（初稿）

- **状态：** DRAFT —— 本文件为初稿，**M9.1 数据清单落地后定稿**（SPEC §10.1）。
- **依据：** `SPEC.md` §10（隐私与安全）、§11（非功能需求-隐私）、`docs/architecture/data-model.md` §4（敏感数据处理）。
- **范围：** MVP 设计（M1 工程基线阶段业务功能未实现，以下内容对应 SPEC 与数据模型已冻结的设计，非实现描述）。

## 1. 概述

matchsong 是一款**本地优先**的 Android 应用：用户录制约 15-30 秒演唱，在设备端完成音频质量检测、音高与音域分析、歌曲推荐。**MVP 无任何网络权限、无后端、无 API Key**（SPEC §10.3、FR-PRIV-3、N-5/N-6），录音音频与声音特征不存在任何上传通道。

## 2. 收集与处理的数据

| 数据类型 | 内容 | 处理目的 | 保存位置 | 保留时间 | 敏感性 |
|---|---|---|---|---|---|
| 录音音频（临时） | 15-30 秒演唱 PCM/WAV | 端侧质量检测与音高分析（分析后不保留） | 应用缓存目录（cacheDir/recordings/，文件缓存） | 分析期：分析完成即删除（FR-PRIV-1）；下次启动清理过期残留（FR-REC-8） | 敏感 |
| 声音特征-音高轨迹 | 逐帧音高（PitchFrame / PitchTrack） | 稳定音域与舒适音区估计 | 仅内存（不持久化） | 分析期 | 敏感 |
| 声音特征-音域统计 | 稳定音域、舒适音区、音高稳定性、置信度（VoiceFeatureVector） | 生成推荐与历史摘要 | Room（本地数据库） | 用户删除前 | 敏感 |
| 推荐结果与解释 | 推荐分数、变调建议、解释文本（含音域信息） | 展示推荐与可解释理由 | Room | 用户删除前 | 敏感 |
| 历史摘要 | 分析时间、音域结果、推荐引用、算法版本（不含原始音频与逐帧轨迹，FR-HX-1） | 历史记录查看 | Room | 用户删除前 | 敏感 |
| 偏好设置 | 语言、风格偏好、数据管理开关 | 候选过滤与个性化 | DataStore | 应用生命周期（重置应用时清除） | 非敏感 |
| 隐私同意记录 | 隐私说明版本、同意时间（ConsentRecord） | 同意管理（FR-ONB-2/3） | DataStore | 应用生命周期（删除全部数据后清除） | 非敏感 |

> 字段级定义（类型/单位/合法范围/来源/保存位置/敏感/保留时间）见 `docs/architecture/data-model.md` §2-§3。

## 3. 处理原则

1. **不上传**：MVP 无网络权限、无后端、无 API Key；不向任何第三方传输录音或声音特征（FR-PRIV-3）。
2. **最小化**：原始音频默认不保存（临时缓存、分析完成即删，FR-PRIV-1）；逐帧音高轨迹不持久化；历史仅存派生摘要（FR-HX-1、data-model.md §4.4）。
3. **敏感判定**：凡由麦克风音频派生的数据（质量报告、音高轨迹、音域统计、推荐分数/解释/结果）一律按「敏感」处理——派生特征仍可能泄露身份（data-model.md §4.1，academic-research §6）；歌曲数据、偏好、同意记录为非敏感。
4. **权限最小化**：仅申请 RECORD_AUDIO + FOREGROUND_SERVICE(+_MICROPHONE)（SPEC §10.5）；任何录音必须伴随可见 UI + 前台通知，不静默录音（FR-REC-9）。
5. **日志脱敏**：Release 日志不含文件名、路径、设备标识、音频内容（FR-PRIV-4；ARCHITECTURE.md §13）。
6. **同意版本化**：同意记录持久化隐私说明版本与同意时间；隐私说明变更后需重新同意（SPEC §10.6、FR-ONB-3）。

## 4. 数据删除

用户可随时在「设置」中执行（FR-HX-4、FR-PRIV-5）：

- 删除单条历史记录；
- 删除全部历史；
- 删除收藏；
- 删除设置（偏好）；
- 删除缓存音频；
- **重置应用**（删除全部数据 = 清空 Room + DataStore + 文件缓存，恢复首次启动状态并重新展示 Onboarding；ACC-15）。

所有删除流程完整可测（FR-PRIV-5）。

## 5. 用户权利

- 本应用无账号系统（单机应用，SPEC N-8），数据全部保存在设备本地；
- 用户可随时删除全部个人数据（见 §4），删除后不可恢复；
- 用户可拒绝隐私同意：未同意时不进入主界面、不请求任何权限、不采集任何音频（ACC-2）；
- 用户可拒绝麦克风权限：拒绝后停留在录音准备页并可重试；永久拒绝后引导至系统设置（FR-REC-5、ACC-3）。

## 6. 合规

Play Store 数据安全表单、隐私政策、麦克风权限用途说明等商店材料在 M9 / M11 准备（SPEC §10.7、PLAN M11.2）。

## 7. 变更与定稿

本文件随产品演进更新：**M9.1 数据清单落地后定稿**；隐私说明版本（ConsentRecord.privacyNoticeVersion）变更时，应用将要求用户重新同意（SPEC §10.6）。
